res = String.length("Abdullah")
IO.puts(res) # 8

res = String.replace("Hello", "e", "a")
IO.puts(res) # Hallo

res = String.duplicate("Click!", 3)
IO.puts(res) # Click!Click!Click!

res = String.split("Hello world!", " ")
IO.inspect(res) # ["Hello", "world!"]
